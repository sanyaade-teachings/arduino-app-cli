name: Complex app
description: Complex app

ports:
  - 7860
  - 8080

bricks:
  - arduino:object_detection: # dep as a map, with additional properties
      model: an-ai-model/yolosuper
      discared-other-prop: 42
      variables:
        MY_FIRST_VARIABLE: a-first-value
        MY_SECOND_VARIABLE: 8080

  - arduino:not_found: # dep as a map, but without additional properties

  - arduino:simple_string # dep as a simple string
